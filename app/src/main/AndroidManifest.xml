<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wayfairproduct.takehome">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name="com.wayfairproduct.takehome.ProductApplication"
        android:allowBackup="true"
        android:icon="@drawable/wayfair"
        android:label="@string/app_name"
        android:roundIcon="@drawable/wayfair"
        android:supportsRtl="true"
        android:theme="@style/Theme.WayfairProduct">
        <activity android:name="com.wayfairproduct.takehome.ui.ProductActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>